import { Move } from 'boardgame.io';

import type { GameState } from '../game-state';

// eslint-disable-next-line @typescript-eslint/no-empty-function
const placeCharacter: Move<GameState> = (): void => {};

export default placeCharacter;
